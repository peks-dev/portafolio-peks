---
interface Props {
  viewBoxSize?: string;
  className?: string;
}

const { viewBoxSize, className } = Astro.props;
---

<svg xmlns="http://www.w3.org/2000/svg" class={className} viewBox={viewBoxSize}>
  <slot />
</svg>

<style>
  svg {
    width: 100%;
    height: 100%;
  }
</style>
